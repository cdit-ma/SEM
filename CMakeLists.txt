cmake_minimum_required (VERSION 2.6)

SET(VERSION_NO "2.2.0")
SET(VERSION "${VERSION_NO}-RE")
SET(URL "https://github.com/cdit-ma/MEDEA")
SET(VERSION_NAME "${PROJ_NAME}v${VERSION}")

add_subdirectory("src")